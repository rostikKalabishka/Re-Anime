import 'package:flutter/material.dart';

class AnimeListsScreen extends StatelessWidget {
  const AnimeListsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
